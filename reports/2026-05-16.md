# 2026-05-16 Remediation

- Removed all active Adsterra slot imports/usages from the home page, tool pages, and individual tool components.
- Deleted the shared Adsterra slot component and removed stale ad setup notes from README/infrastructure metadata.
- Removed sponsored partner cards so clicks keep users inside the selected tool flow.
- Preserved GA4 `G-G0YE8ZCN66`, sitemap, robots, and browser-only tool behavior for monitoring.

## Functional Tool Index Fix

- Removed unimplemented `Coming soon` tools from the indexed registry so sitemap URLs only point to pages with working controls.
- Fixed home cards to link by tool slug instead of internal IDs, preventing broken tool clicks.
- Added a real `/ko/tools` index page and moved category navigation to `/ko/tools#categories`.
- Replaced the old "50+ tools" claim with the currently working tool set to avoid overpromising and low-trust UX.
- Cleaned stale structured-data copy and corrupted title/keyword metadata.
